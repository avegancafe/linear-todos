# linear-todos

A todo manager built on Linear, available in two forms that share the same
concepts (smart natural-language dates, priorities, urgency grouping):

1. **Python CLI** (repo root) — the original. An agent-skill / CLI that talks to
   Linear's GraphQL API directly. Still used standalone and for cron digests.
2. **Raycast extension** (`raycast-extension/`) — a native TypeScript port that
   talks to Linear via `@linear/sdk` + Raycast OAuth. Store-oriented; no Python
   dependency.

The two are independent: the Raycast extension does **not** call the Python CLI.
It reimplements the relevant logic (date parsing, API calls) in TypeScript.

## Repository layout

```
.
├── main.py                  # Python CLI entry point (click)
├── src/linear_todos/        # Python package
│   ├── cli.py               # click commands: create, list, done, snooze,
│   │                        #   remind, review, digest, setup, cron
│   ├── api.py               # Linear GraphQL client
│   ├── config.py            # ~/.config/linear-todos/config.json + LINEAR_* env
│   ├── dates.py             # natural-language date parsing (source of truth)
│   ├── setup_wizard.py      # interactive setup
│   └── cron_manager.py      # cron digest scheduling (CLI-only feature)
├── tests/                   # pytest suite for the Python CLI
├── pyproject.toml           # Python project (uv, hatchling)
├── SKILL.md                 # agent-skill manifest for the Python CLI
├── cron-jobs.txt            # example system crontab entries
└── raycast-extension/       # native TypeScript Raycast extension
    ├── package.json         # Raycast manifest (commands, prefs, deps)
    ├── src/
    │   ├── list.tsx         # "List Todos" — grouped-by-urgency view
    │   ├── remind.tsx       # "Remind Me" — no-view natural-language quick-add
    │   ├── setup.tsx        # "Setup" — pick team + workflow states
    │   └── lib/
    │       ├── oauth.ts     # OAuthService.linear + shared LinearClient
    │       ├── linear.ts    # @linear/sdk data layer (replaces api.py)
    │       ├── dates.ts     # TS port of dates.py + urgency bucketing
    │       ├── settings.ts  # LocalStorage config + auto-resolution
    │       ├── errors.tsx   # toast / Detail error UI
    │       └── __tests__/   # vitest tests for dates.ts
    └── assets/icon.png      # extension icon
```

## Conventions & workflows

### Python CLI
- Managed with `uv`. Run commands via `uv run python main.py <cmd>`.
- Tests: `uv run pytest`.
- Auth: `LINEAR_API_KEY` env var or `~/.config/linear-todos/config.json`.

### Raycast extension (`raycast-extension/`)
- `npm install`, then `npm run dev` (`ray develop`) to load it in Raycast.
- Verify before committing: `npm run lint` (ray lint), `npm run build`
  (ray build, type-checks all entry points), `npm test` (vitest).
- Auth is OAuth via Raycast — no API keys stored by the extension.
- Team/state config lives in Raycast LocalStorage; set via the Setup command.
- `dist/`, `node_modules/`, `.raycast/` are gitignored — never commit them.
- `raycast-env.d.ts` is generated by `ray build`; it is committed.

### Behavior notes
- `dates.py` is the canonical date-parsing spec; `dates.ts` mirrors it (and the
  vitest suite encodes that parity). Keep them in sync when changing parsing.
- Reminder syntax (both CLI and extension): natural-language date phrases plus a
  trailing `!<priority>` marker — `!urgent`/`!important`, `!high`, `!medium`,
  `!low`, `!none` — which is stripped from the title.
- Cron digests are a **Python-CLI-only** feature; Raycast has no cron equivalent.

## Git
- Conventional Commits style (`feat:`, `fix:`, `docs:`, `style:`, `chore:`).
- Only commit/push when explicitly asked.
